{
    "patch_policy": [""],
    "patch_nodes": [{
        "container": {
            "snapshot": "ss-t5crtyv0",
            "zone": "pek3"
        },
        "patch": [{
            "mount_role": "replica",
            "mount_point": "/upgrade",
            "mount_options": "defaults,noatime",
            "filesystem": "ext4",
            "cmd": "/upgrade/opt/mongodb/bin/upgrade.sh"
        }],
        "rollback": [{
            "mount_role": "replica",
            "mount_point": "/upgrade",
            "mount_options": "defaults,noatime",
            "filesystem": "ext4",
            "cmd": "/upgrade/opt/mongodb/bin/upgrade.sh rollback"
        }]
    }]
}
